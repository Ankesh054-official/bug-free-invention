'use strict';
/*
//STRICT MODE:

let hasDriversLicence = false;
const testPass = true;

if(testPass) hasDriversLicence = true;
if(hasDriversLicence) console.log(hasDriversLicence);

// let interface = true;
// let private = true;
// let public = true;



//FUNNCTION

function logger(){
    console.log('hello');
}

logger()
logger()
logger()
logger()


//DATA STRUCTURE 

// ARRAY

//BASIC OPERATION

const age = [21, 19, 20];
console.log(age);

//Add Elements

const indexOfAge = age.push(22);

console.log(age,indexOfAge);

console.log(age.indexOf(22));

const ankesh = {
    firstName: 'ANKESH',
    lastName: 'SHARMA',
    age: 2022-2001,
    job:'Software Engineer',
    friends:['Inderjeet', 'Shubham', 'Rahul', 'Neeraj']
};

console.log(`I am ${ankesh.firstName} ${ankesh.lastName}. A ${ankesh.job} of ${ankesh.age} years of age. I have ${ankesh.friends.length} friends and my best friend is ${ankesh.friends[0]}.`);



// Objects Methods

const vari = {
    name:'ANKESH',
    class: 6,
    hasGirlfriend: false,
    hasCrush: !this.hasGirlfriend,
    hasCrushOn: !this.hasCrush? 'RITIKA': '',
    groupsInSchool:{
        name:'ANKESH',
        friends:['INDERJEET ', 'SHUBHAM ', 'RAHUL ', 'NEERAJ ', 'HARSH '],
        anushashan:'KESHAV, ',
        ahinsha:'HIMANSHU, ',
        satya:'RAHUL ROY, ',
        dharya: `RITIKA.`,
        listMembersNameOfYourGroup:function (){
            return `${this.dharya}${this.name}, ${String(this.friends)}`;
        }
        
    }
};

console.log(`${vari.name} is in class ${vari.class} and he has ${vari.hasGirlfriend? 'a ':'not a '}girlfriend. ${vari.hasCrush?'but has a crush on ':'don\'t even has a crush ' } ${vari.hasCrushOn}. And he has ${vari.groupsInSchool.friends.length} friends, They are ${String(vari.groupsInSchool.friends)}.`);

console.log(vari.groupsInSchool.listMembersNameOfYourGroup());

const table = Number(prompt());

for(let i = 1; i <= 10; i++){
    console.log(i*table);
}

let jonas = [1, 'ankesh', true, {'hello':1},[1,2,'s'], NaN, null, undefined];

const typeOfElements = [];

console.log(jonas.length);
for(let i = 0; i < jonas.length; i++){
    // console.log(jonas[i], typeof(jonas[i]));
    // typeOfElements[i] = typeof(jonas[i]);
    // typeOfElements.push(typeof(jonas[i]));
    // typeOfElements.unshift(jonas[i]);

    console.log(jonas.shift());
    console.log(jonas);
}

console.log(jonas);


// console.log(typeOfElements);*/   

// BUGS